# Runtime dependencies for hermes-openai-proxy.
#
# Install with:
#   pip install -r requirements.txt
#
# Or use the project metadata:
#   pip install hermes-openai-proxy

fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0
pydantic>=2.5